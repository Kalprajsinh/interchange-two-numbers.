#include<stdio.h>

void main()
{
  int x, y,z;

  printf("Enter  first number: ");
  scanf("%d", &x);
  printf("Enter second number: ");
  scanf("%d", &y);

  x=x+y; 
  y=x-y;
  x=x-y;
  
  printf("\nAfter swap : x=%d y=%d",x,y);   
  
}
